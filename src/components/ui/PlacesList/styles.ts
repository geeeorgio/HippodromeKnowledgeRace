import { StyleSheet } from 'react-native';

import { hp } from 'src/utils';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: hp(20),
  },
  contentContainer: {
    gap: hp(20),
    paddingBottom: hp(40),
  },
});
